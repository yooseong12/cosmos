package com.toy.cosmos.domain.repository.custom.impl;

import com.toy.cosmos.domain.repository.custom.UserFriendCustomRepository;
import lombok.RequiredArgsConstructor;

@Deprecated
@RequiredArgsConstructor
public class UserFriendCustomRepositoryImpl implements UserFriendCustomRepository {

//    private final EntityManager entityManager;
//
//    @Override
//    public UserFriend findBy(String email, String phone) {
//        return new JPAQuery<UserFriend>(entityManager)
//                .from(userFriend)
//                .join(user).on(user.id.eq(userFriend.friendId))
//                .where(ObjectUtils.isEmpty(email) ? user.phone);
//    }
//
//    private BooleanBuilder a(String email, String phone)
}
